spring:
  application:
    name: gateway-service

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8000}
  shutdown: graceful

app-gateway:
  open-paths:
    - /api/v1/auth/login
    - /api/v1/auth/register
    - /api/v1/auth/hello
    - /actuator/health
    - /swagger-ui.html
    - /swagger-ui/**
    - /v3/api-docs/**
    - /webjars/**

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY:must-be-overridden}

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    urls:
      - name: User Service
        url: /v3/api-docs/user-service
  api-docs:
    enabled: true
    path: /api-docs

logging:
  level:
    root: INFO